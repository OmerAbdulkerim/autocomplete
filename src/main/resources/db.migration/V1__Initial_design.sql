/*
Alter permissions regulations and File path Accordingly. 
*/

/*CREATE TABLE IF NOT EXISTS contact (
    name TEXT NOT NULL,
    phone_number TEXT NOT NULL PRIMARY KEY
);

COPY contact(name, phone_number)
    FROM 'path/phone_numbers.csv'
    DELIMITER ','
    CSV HEADER;
*/
